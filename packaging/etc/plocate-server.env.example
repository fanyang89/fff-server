# plocate-server configuration.
#
# Copy this file to /etc/plocate-server/plocate-server.env and edit.
# All variables are optional; the systemd units ship sane defaults.
# Every flag has a matching env var; see `plocate-server --help`.

# Root directory to index (REQUIRED at first run; updatedb walks it).
PLOCATE_SERVER_BASE_PATH=/srv/files

# HTTP bind address.
PLOCATE_SERVER_BIND=127.0.0.1:8787

# On-disk plocate database path (created/refreshed by updatedb).
PLOCATE_SERVER_DB_PATH=/var/lib/plocate-server/files.db

# Override the plocate / updatedb binaries (default: the vendored musl ones).
PLOCATE_SERVER_PLOCATE_BIN=/usr/libexec/plocate-server/plocate
PLOCATE_SERVER_UPDATEDB_BIN=/usr/libexec/plocate-server/updatedb

# Seconds between in-process reindex runs. 0 disables (the systemd timer
# plocate-server-updatedb.timer is the primary refresh mechanism).
PLOCATE_SERVER_REINDEX_INTERVAL_SECS=0

# Maximum results returned by a single search/glob call.
PLOCATE_SERVER_MAX_RESULTS=100
