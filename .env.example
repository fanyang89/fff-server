# Contact email shown in the web UI's feedback entry.
# Leave unset to hide the feedback link entirely.
# Loaded via your process manager (e.g. systemd EnvironmentFile) or shell export.
PLOCATE_SERVER_FEEDBACK_EMAIL=
